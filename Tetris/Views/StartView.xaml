<UserControl x:Class="Tetris.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tetris.Views"
             mc:Ignorable="d" 
            
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Image Source="/Images/Tetris.jpg" />
        <!--遮罩-->
        <Border Background="#66828282"/>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <StackPanel>
                <!--游戏名-->
                <TextBlock  Text="Tetris" HorizontalAlignment="Center" FontSize="50" FontWeight="Bold" FontFamily="KaiTi" Foreground="White">
                    <TextBlock.Effect>
                        <DropShadowEffect Direction="305" ShadowDepth="4" BlurRadius="4"/>
                    </TextBlock.Effect>
                </TextBlock>
                <!--分割线-->
                <Rectangle Fill="#F2F2F2"  Height="1" Margin="10 0"/>
                <Button x:Name="Start" Content="START"/>
                <Button Content="OPPTION"/>
                <Button Content="HELP"/>
            </StackPanel>
        </Grid>
        <ContentControl x:Name="ActiveItem"></ContentControl>
    </Grid>
</UserControl>
